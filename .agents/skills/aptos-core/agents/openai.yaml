interface:
  display_name: "Aptos Core"
  short_description: "Repo-specific patterns and workflows for aptos-core"
  default_prompt: "Use the aptos-core repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true